services:
  postgres:
    image: postgres:16-alpine
    container_name: accounterp-postgres
    environment:
      POSTGRES_DB: accounterp
      POSTGRES_USER: accounterp
      POSTGRES_PASSWORD: accounterp
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: accounterp-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  postgres-data:
  redis-data:
